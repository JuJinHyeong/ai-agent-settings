[project]
name = "dummy-app"
version = "0.1.0"
description = "Dummy app for Windows Toast notifications with custom icons"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyinstaller>=6.0.0",
]
